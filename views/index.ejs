<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gbolahan's Cloud Store</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .hero { background: #232f3e; color: white; padding: 60px 0; margin-bottom: 30px; }
        .card-img-top { height: 200px; object-fit: cover; }
        .price { font-size: 1.25rem; font-weight: bold; color: #232f3e; }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="#">☁️ CloudStore</a>
            <button class="btn btn-outline-light btn-sm">🛒 Cart (<span id="cart-count">0</span>)</button>
        </div>
    </nav>
    <div class="hero text-center">
        <h1>Welcome to the Cloud Infrastructure Demo</h1>
        <p>Running on AWS ECS Fargate • Deployed via Terraform</p>
    </div>
    <div class="container">
        <div class="row">
            <% products.forEach(function(product) { %>
                <div class="col-md-3 mb-4">
                    <div class="card h-100 shadow-sm">
                        <img src="<%= product.img %>?auto=format&fit=crop&w=500&q=60" class="card-img-top" alt="<%= product.name %>">
                        <div class="card-body d-flex flex-column">
                            <h5 class="card-title"><%= product.name %></h5>
                            <p class="price">$<%= product.price %></p>
                            <button class="btn btn-primary mt-auto" onclick="addToCart()">Add to Cart</button>
                        </div>
                    </div>
                </div>
            <% }); %>
        </div>
    </div>
    <footer class="text-center py-4 text-muted border-top mt-5">
        <p>Architected by Gbolahan Adekanbi | OCI & AWS Certified</p>
    </footer>
    <script>
        let count = 0;
        function addToCart() {
            count++;
            document.getElementById('cart-count').innerText = count;
            alert("Item added to cart!");
        }
    </script>
</body>
</html>
